# Kubernetes Secret for K8s Challenge App
# ========================================
# TODO: Complete this Secret
#
# Requirements:
# - Name must be: k8s-challenge-secrets
# - Include an api-key value
# - Values must be base64 encoded
#
# Hints:
# - See README.md Step 4 for detailed guidance
# - To encode: echo -n "your-value" | base64
# - To decode: echo "encoded-value" | base64 -d
# - NEVER commit real secrets to git!

apiVersion: v1
kind: Secret
metadata:
  name: k8s-challenge-secrets
type: Opaque
data:
  # TODO: Add your base64-encoded secret
  # Example: "my-api-key-123" → "bXktYXBpLWtleS0xMjM="
  #
  # Generate your own:
  #   echo -n "your-api-key-here" | base64
  #
  # On Windows PowerShell:
  #   [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("your-api-key-here"))

  api-key: REPLACE-WITH-BASE64-ENCODED-VALUE
